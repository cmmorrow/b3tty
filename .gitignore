.DS_Store
b3tty
b3tty.exe
*.yaml
TODO*
src/index.html
.zed
# Generated by `make client` (bun build output); go:embed requires the
# directory to exist at compile time, so `make client` is a prerequisite of
# `test`/`test-race`/`build` rather than something CI restores from git.
src/dist/
commandline-settings-research.md
best-practice-violations.md
